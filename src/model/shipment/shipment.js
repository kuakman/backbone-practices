/**
 * @module model.shipment
 **/
const _ = require('util/mixin');
const Backbone = require('backbone');

const ShipmentModel = Backbone.Model.extend({

	initialize: function() {
		return this;
	}

});

module.exports = ShipmentModel;
